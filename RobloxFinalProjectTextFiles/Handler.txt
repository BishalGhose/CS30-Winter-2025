local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ServerStorage = game:GetService("ServerStorage")
local MainModuleFunctions = require(ServerStorage.MainModuleFunctions)
local TerrainDecorationsManager = require(ServerStorage.TerrainDecorationsManager)
local ProceduralFunctions = require(ServerStorage.ProceduralFunctions)

MainModuleFunctions.makeMap()
TerrainDecorationsManager.decorateMap()

while task.wait(10) do
	
	MainModuleFunctions.deleteMap()
	
	local MainModuleFunctions = require(ServerStorage.MainModuleFunctions)
	local TerrainDecorationsManager = require(ServerStorage.TerrainDecorationsManager)
	local rainbowHighlight = ReplicatedStorage:FindFirstChild("RainbowHighlight")
	rainbowHighlight.Parent = workspace.RainbowParts
	MainModuleFunctions.makeMap()
	TerrainDecorationsManager.decorateMap()
	
end

